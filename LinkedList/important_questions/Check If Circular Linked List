/***Companies: microsoft***/
/***GFG***/

bool isCircular(Node *head)
{
   // Your code here
   if(head==NULL) return 1;
   Node* temp = head->next;
   while(temp!=NULL && temp!=head){
       temp = temp->next;
   }
   return (temp==head);
}
